# Allow requests from local dev server and deployed Cloud Run frontend services
cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# Configure file upload size limits (e.g., 10MB)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
